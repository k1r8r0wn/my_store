%h1 Items
- if @items.empty?
  %b No items in the store
- else
  %table
    %thead
      %tr
        %th ID
        %th Image
        %th Name
        %th Created at
        %th Price
        %th Delete?
        %th Raiting
    %tbody
      - @items.each do |item|
        %tr
          %td= item.id
          %td= image_tag item.image, width: '50px', height: '50px' if item.image.present?
          %td= link_to item.name, item_path(item)
          %td #{item.created_at}$
          %td #{item.price}$
          %td= link_to 'Delete', item_path(item, admin: params[:admin]), method: :delete, data: { confirm: 'Are you sure?' }
          %td #{item.votes_count}, #{link_to '+', upvote_item_path(item)}
